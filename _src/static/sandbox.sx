const sandboxImports = require("./sandbox-imports");

module.exports = class {
	
	render({root, version}) {
		<:head>
			<title>Sandbox — Sactory v${version}</title>
			<[sandboxImports] $root=root $dist=`${root}dist/` />
		</:head>
		return <#app $name="Sandbox" $src="./dist/sandbox.js" />
	}

};
